# ZLIB stuff
ZLIB_VERSION		= 1.2.3
ZLIB_URI			= http://www.zlib.net
ZLIB_TARGET			= 3rd_party
ZLIB_FOLDER			= $(ZLIB_TARGET)/zlib-$(ZLIB_VERSION)
ZLIB_TARBALL		= zlib-$(ZLIB_VERSION).tar.gz

# NEWT stuff
NEWT_VERSION		= 0.52.10
NEWT_URI			= https://fedorahosted.org/releases/n/e/newt
NEWT_TARGET			= 3rd_party
NEWT_FOLDER			= $(NEWT_TARGET)/newt-$(NEWT_VERSION)
NEWT_TARBALL		= newt-$(NEWT_VERSION).tar.gz

# SLANG stuff
SLANG_VERSION		= 2.2.1
SLANG_URI			= ftp://space.mit.edu/pub/davis/slang/v2.2/
SLANG_TARGET		= 3rd_party
SLANG_FOLDER		= $(SLANG_TARGET)/slang-$(SLANG_VERSION)
SLANG_TARBALL		= slang-$(SLANG_VERSION).tar.gz

# ReiserFS stuff
REISER_VERSION		= 3.x.0k
REISER_URI			= http://ftp.ens-cachan.fr/ftp/mirror/ReiserFS/reiserfsprogs
REISER_TARGET		= 3rd_party
REISER_FOLDER		= $(REISER_TARGET)/reiserfsprogs-$(REISER_VERSION)
REISER_TARBALL		= reiserfsprogs-$(REISER_VERSION).tar.gz

# E2FS stuff
E2FS_VERSION		= 1.22
E2FS_URI			= http://downloads.sourceforge.net/project/e2fsprogs/e2fsprogs/1.22
E2FS_TARGET			= 3rd_party
E2FS_FOLDER			= $(E2FS_TARGET)/e2fsprogs-$(E2FS_VERSION)
E2FS_TARBALL		= e2fsprogs-$(E2FS_VERSION).tar.gz

