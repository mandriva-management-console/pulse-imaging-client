# NEWT stuff
NEWT_VERSION	= 0.52.10
NEWT_URI		= https://fedorahosted.org/releases/n/e/newt
NEWT_FOLDER		= newt-$(NEWT_VERSION)
NEWT_TARBALL	= newt-$(NEWT_VERSION).tar.gz

# SLANG stuff
SLANG_VERSION	= 2.2.1
SLANG_URI		= ftp://space.mit.edu/pub/davis/slang/v2.2/
SLANG_FOLDER	= slang-$(SLANG_VERSION)
SLANG_TARBALL	= slang-$(SLANG_VERSION).tar.gz

# ReiserFS stuff
REISER_VERSION	= 3.x.0i-linbox01
REISER_URI		= http://ftp.ens-cachan.fr/ftp/mirror/ReiserFS/reiserfsprogs
REISER_FOLDER	= reiserfsprogs-$(REISER_VERSION)
REISER_TARBALL	= reiserfsprogs-$(REISER_VERSION).tar.gz

# E2FS stuff
E2FS_VERSION	= 1.22
E2FS_URI		= http://downloads.sourceforge.net/project/e2fsprogs/e2fsprogs/1.22
E2FS_FOLDER		= e2fsprogs-$(E2FS_VERSION)
E2FS_TARBALL	= e2fsprogs-$(E2FS_VERSION).tar.gz

# POPT
POPT_VERSION	= 1.15
POPT_URI		= http://rpm5.org/files/popt
POPT_FOLDER		= popt-$(POPT_VERSION)
POPT_TARBALL	= popt-$(POPT_VERSION).tar.gz

# MEMTEST86+
MEMTEST_VERSION = 4.00
MEMTEST_URI     = http://www.memtest.org/download/$(MEMTEST_VERSION)
MEMTEST_FOLDER  = memtest86+-$(MEMTEST_VERSION)
MEMTEST_TARBALL = memtest86+-$(MEMTEST_VERSION).tar.gz

